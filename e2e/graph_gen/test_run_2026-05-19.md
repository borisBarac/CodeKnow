# E2E Hybrid Search Test Run — 2026-05-19

**Config:** `traversal_depth=3` (`E2E_TRAVERSAL_DEPTH`), `n_results=10`
**Threshold:** `final_score >= 60`

## Non-judge tests: 5/5 PASSED

- `test_hybrid_search_returns_response`
- `test_hybrid_search_has_vector_hits`
- `test_hybrid_search_results_have_required_fields`
- `test_hybrid_search_results_sorted_by_provenance`
- `test_hybrid_search_graph_expansion`

## Judge tests: 3/4 PASSED, 1/4 FAILED

---

### 1. Post creation flow — how does creating a new post work end to end

| Metric | Value |
|---|---|
| **Final Score** | **70.8 / 100** |
| Confidence | medium |
| Winner | semantic_only |
| **Result** | **PASSED** (70.8 >= 60) |

**Subscores:**

| Subscore | Value |
|---|---|
| semantic_relevance | 85 |
| kg_expansion_value | 20 |
| coverage | 75 |
| groundedness | 100 |
| noise_control | 70 |

**Evidence used:** semantic: 10 hits · graph: 2 hits · merged: 12 hits

**Strengths:**
- Semantic hits capture the core frontend form, tRPC mutation call, server-side add procedure, database insert, and event emission.
- The `post.ts` snippet includes the full `add` mutation logic and the start of the `onAdd` subscription, covering the server-side flow well.
- Agent analysis is factually accurate and makes no unsupported claims.

**Weaknesses:**
- Graph expansion adds minimal value: one low-weight method on an event emitter (0.3) and a redundant contains edge (0.15).
- Coverage is incomplete for the client-side SSE subscription (`useLivePosts` hook) and the full `onAdd` procedure.
- Some semantic hits are off-topic (`create-channel.tsx`, `channel.ts` imports, `route.ts` re-export) introducing noise.
- Agent analysis is only a summary of files, not an actual end-to-end explanation.

**Missing evidence:**
- Client-side SSE subscription logic (`useLivePosts` hook) not shown.
- Full `onAdd` procedure implementation not retrieved.
- Database schema details beyond `PostType` export missing.

**Rationale:**
Semantic hits are highly relevant (85), covering the frontend form, mutation call, server-side add procedure, and event emission. Graph expansion adds little value (20): one low-weight method (0.3) on an event emitter and a redundant contains edge (0.15). Coverage is decent (75) but misses the client-side SSE subscription and full `onAdd` procedure. Groundedness is perfect (100) because the agent analysis only lists files and makes no unsupported claims. Noise control is moderate (70) due to a few off-topic hits. Overall, semantic retrieval alone suffices; graph expansion is not beneficial, so winner is `semantic_only`.

---

### 2. Adding a message — what happens when a user sends a message

| Metric | Value |
|---|---|
| **Final Score** | **44.0 / 100** |
| Confidence | medium |
| Winner | semantic_only |
| **Result** | **FAILED** (44.0 < 60) |

**Subscores:**

| Subscore | Value |
|---|---|
| semantic_relevance | 70 |
| kg_expansion_value | 10 |
| coverage | 30 |
| groundedness | 20 |
| noise_control | 85 |

**Evidence used:** semantic: 10 hits · graph: 1 hits · merged: 11 hits

**Strengths:**
- Semantic hits are tightly focused on the message-sending UI and client-side logic
- The `postMessage` function and tRPC mutation call are clearly captured

**Weaknesses:**
- No server-side code (tRPC router, SSE, database) retrieved
- Graph expansion added only noise (`providers.tsx` with `'use client'`)
- Agent analysis provides no actual explanation of what happens
- Evidence is limited to a single file, missing the broader flow

**Missing evidence:**
- Server-side tRPC router for `post.add` mutation
- SSE subscription and broadcasting logic
- Database interaction (Drizzle ORM) for persisting messages
- Authentication/authorization checks on the server

**Rationale:**
Semantic hits are all from the `chat.tsx` file and cover the client-side message sending flow (`AddMessageForm`, `postMessage`, tRPC mutation call, `onSuccess`/`onError`). They are relevant but incomplete for the query, which likely expects the full end-to-end flow. The graph expansion added a single hit from `providers.tsx` via a weak `contains` relation (weight 0.15) with a snippet that is just `'use client'`; this is noise and provides no incremental value. Coverage is poor because the server-side logic, SSE broadcasting, and database operations are entirely missing. The agent analysis is just a list of files and lines, with no grounded reasoning. Noise is low overall, but the graph hit is irrelevant. The final score reflects the lack of server-side evidence and the negligible graph contribution.

---

### 3. Typing indicator — how does the typing indicator work

| Metric | Value |
|---|---|
| **Final Score** | **81.3 / 100** |
| Confidence | high |
| Winner | semantic_only |
| **Result** | **PASSED** (81.3 >= 60) |

**Subscores:**

| Subscore | Value |
|---|---|
| semantic_relevance | 92 |
| kg_expansion_value | N/A |
| coverage | 78 |
| groundedness | 100 |
| noise_control | 85 |

**Evidence used:** semantic: 10 hits · graph: 0 hits · merged: 10 hits

**Strengths:**
- Semantic hits capture the core server-side state management and cleanup logic
- Client subscription and UI rendering are clearly shown
- Event interface and import in `post.ts` provide context for the broadcast mechanism

**Weaknesses:**
- Missing the client-side hook that actually sets the typing state (only a comment retrieved)
- Some duplication in semantic hits (same line from `chat.tsx` appears multiple times)
- The exact interaction between `post.ts` and the typing indicator is not fully revealed

**Missing evidence:**
- Client-side hook implementation that sets `isTyping` (only a comment was retrieved)
- Exact usage of `currentlyTyping` in `post.ts` (only imports shown)
- The tRPC procedure that updates the typing state on the server

**Rationale:**
Semantic hits are highly relevant, directly covering the typing indicator's data structure (`WhoIsTyping`, `currentlyTyping`), server-side cleanup interval, event emission, client subscription, and UI rendering. Some duplication exists (multiple snippets of the same line) but overall focus is strong. Graph expansion returned no hits, indicating semantic saturation; therefore `kg_expansion_value` is null and the winner is `semantic_only`. Coverage is good but not complete: the client-side hook that sets typing is only hinted at by a comment, and the exact role of `post.ts` in clearing typing is not shown. The agent analysis is minimal (a file list) but fully grounded in the evidence. Noise is low with only minor redundancy. Final score 81.3 reflects strong semantic relevance and groundedness, slightly tempered by incomplete coverage of the full typing flow.

---

### 4. Auth guard — how does authentication guard the tRPC procedures

| Metric | Value |
|---|---|
| **Final Score** | **76.0 / 100** |
| Confidence | medium |
| Winner | semantic_only |
| **Result** | **PASSED** (76.0 >= 60) |

**Subscores:**

| Subscore | Value |
|---|---|
| semantic_relevance | 85 |
| kg_expansion_value | N/A |
| coverage | 70 |
| groundedness | 75 |
| noise_control | 75 |

**Evidence used:** semantic: 8 hits · graph: 3 hits · merged: 10 hits

**Strengths:**
- `authedProcedure` guard logic directly retrieved via semantic search
- `createContext()` and `context.ts` show session propagation origin
- tRPC handler showing endpoint wiring is present
- Good recall of auth-related files (`trpc.ts`, `context.ts`, `auth.tsx`, `_app.ts`)
- Core auth chain is well-covered without requiring graph expansion

**Weaknesses:**
- KG expansion only added coarse `contains` edges (0.15 weight) — no `calls`, `uses`, or `inherits` edges to enrich the auth graph
- Coverage is slightly incomplete: no usage sites of `authedProcedure` in routers are shown, so it's impossible to confirm which procedures are actually guarded
- Agent analysis lists files but does not interpret how auth actually guards procedures — it's more a summary than an analysis
- Some noise: `RootLayout` snippet is bloated with JSX and tangential auth import (layout-level `SessionProvider`)

**Unsupported claims:**
- The agent analysis does not explicitly describe how authentication guards procedures beyond listing files — no step-by-step explanation of session-check → UNAUTHORIZED error flow.
  - **Reason:** `agent_analysis` merely summarizes retrieved files and paths; groundedness thus depends on whether we infer that the evidence supports an answer, not that the agent actually connected the dots.

**Missing evidence:**
- Which routers/procedures use `authedProcedure` vs `publicProcedure`
- Direct import or reference of `auth()` from tRPC context
- Edge connecting `trpc.ts` `authedProcedure` to router definitions in `channel`/`post` routers

**Rationale:**
Semantic search directly retrieved the `authedProcedure` guard, context creation, and auth imports (high relevance, 85). Graph expansion only added `contains` relations (weight 0.15) that did not reveal new functional linking — e.g., no `calls` from `authedProcedure` to `auth` or from routers to `authedProcedure`. KG expansion value is set to null per semantic saturation guideline: the expansion did not provide high-value incremental evidence. Coverage (70) is adequate for explaining the guard mechanism but missing which procedures are protected. Groundedness (75) reflects that the agent analysis lists relevant files but doesn't synthesize how auth guards procedures — the evidence is present but under-utilized. Noise (75) from long layout snippet and duplicate container hits. Since semantic hits carry the key authentication logic and graph expansion adds negligible value, winner is `semantic_only`.

---

## Summary

| Query | Score | Result |
|---|---|---|
| post creation flow | 70.8 | PASS |
| adding a message | 44.0 | **FAIL** |
| typing indicator | 81.3 | PASS |
| auth guard | 76.0 | PASS |

- **Pass rate:** 3/4 judge tests (75%)
- **Average score:** 68.0
- **All winners:** `semantic_only` (graph expansion not beneficial for any query)
